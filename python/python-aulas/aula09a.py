#Metodos - 

# len(varíavel) = quantidade de caracteres da varíavel
# count() = quantidade de vezes que a letra aparece
# find() = fala o indice onde a palavra começa
# 'curso' in frase = retorna um valor boolean se a string existe na varíavel
# replace() = troca o texto selecionado por outro texto
# upper() = transforma tudo em maiusculo
# lower() = transforma tudo em minusculo
# capitalize() = transforma tudo em minusculo, e a primeira letra em maiusculo
# title() = transforma tudo em minusculo, e a primeira letra de todas as palavras em maiusculo
# strip tira todos os espaços da esquerda e direita
# lstrip tira todos os espaços da esquerda
# rstrip tira todos os espaços da direita
# split separa cada palavra em um bloco, transformando a varíavel em uma "lista" []
# '-'.join(frase) = juntar uma lista de palavras com algo, no exemplo é o '-'
